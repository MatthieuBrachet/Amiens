function [ f ] = second_membre(x,y,t,U,U0)
global test
if test == 1
    f=0;
elseif test == 2
    lambda=10000;
    [n1,n2]=size(x);
    for i=1:n1
        for j=1:n2
            if 
            xi(i,j)=
    f=lambda*
end
end

